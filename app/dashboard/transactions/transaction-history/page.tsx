import TransactionHistory from '@/app/ui/transactions/transaction-history/transaction-history';

export default async function Page() {
    return (
        <TransactionHistory userId="yourUserId" />
    );
}
